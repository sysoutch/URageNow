If the user wants you to do something you're not trained on, check the markdown files in `.agents` and `.agents/skills/`.
on session start, always read the memory bank.
Session start rules and Taskitty task-recording workflow live in `.agents/skills/taskitty/SKILL.md` (single source of truth) — read it before acting.