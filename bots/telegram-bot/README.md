# Telegram Bot

This runtime can be started from the dashboard Messenger Runtime panel or manually with `npm run start` in this folder.

## Credentials

Set `TELEGRAM_BOT_TOKEN` for service and CI deployments. For an interactive user-run process, store the token as `telegram.default.bot-token` in the operating system credential store instead. The environment variable takes priority when both are present.
